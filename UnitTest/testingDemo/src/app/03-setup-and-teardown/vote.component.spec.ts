import { VoteComponent } from './vote.component'; 

describe('VoteComponent', () => {
  let component:VoteComponent;
  beforeEach(()=>{
    //fot initializing out objects or setting up each test
    component = new VoteComponent();
  });
  afterAll(()=>{
    //for clean up each test
  });
  //executed once before all test
  beforeAll(()=>{});
  //executed once after all test
  afterAll(()=>{});
  it('should increament totalVotes when upVoted ', () => {
  });

  it('', () => {
  });
});